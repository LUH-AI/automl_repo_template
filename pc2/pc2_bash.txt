
export REPODIR=$SCRATCH"/projects"
alias cluster-info="sinfo -o '%8P %10n %.11T %.4c %.8z %.6m %12G %10l %10L %10O %20E' -S '-P'"
alias make-project="conda activate template && cd $REPODIR && sh automl_repo_template/cookiecutter.sh"
export ENVDIR=$SCRATCH"/envs"
alias conda-create="conda create -p $ENVDIR"
alias conda-activate="conda activate $ENVDIR"

# Sync to/from cluster, relative to repodir
# e.g. from cluster to local: rsync2local runs .
alias rsyn2local="rsync -azv --delete -e 'ssh -J $USER@fe.noctua2.pc2.uni-paderborn.de' $USER@n2login5:$REPODIR/$1 $2"
alias rsyn2cluster="rsync -azv --delete -e 'ssh -J $USER@fe.noctua2.pc2.uni-paderborn.de' $1 $USER@n2login5:$REPODIR/$2"